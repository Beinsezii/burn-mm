[package]
name = "burn-mm"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.18.0", features = ["vulkan"], git = "https://github.com/tracel-ai/burn.git", branch = "chore/matmul-selector" }
